import { titleFont } from "@/config/fonts";

function Home() {
  return (
    <main>
      <h1 className={`${titleFont.className} font-bold `}>Hola Mundo</h1>
    </main>
  );
}

export default Home;
